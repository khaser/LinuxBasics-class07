name: 'Task04'
description: 'Creating a file'
inputs:
  filename:
    required: true

outputs:
  time:
    decsription: "Descri"

runs:
  steps:
    - shell: bash
      run: touch ${{ inputs.filename }} 
