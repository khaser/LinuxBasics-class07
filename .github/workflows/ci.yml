name: LinuxCourse Workflow

on:
  - push
  - pull_request

jobs:

  task07:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"

  build:
    name: Autograding
    runs-on: ubuntu-latest
    needs:
      - task07
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Task 1
        run: sudo apt install cmatrix
      - name: Task 2
        run: ghc --version | tee ghc-version.txt
      - name: Task 4
        uses: ./.github/actions
        with:
          filename: task04-result
      - run: echo "autograding emulator"
